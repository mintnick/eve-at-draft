# Raw Tournament Data: 2021

- `source.json` stores the normalized raw tournament source fetched from official upstream artifacts
- `overrides.json` is reserved for manual corrections if an upstream source needs patching
- `sources/` stores fetched official source artifacts used to build `source.json`
