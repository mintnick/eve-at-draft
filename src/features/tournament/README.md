# Tournament Feature

Tournament selection and year-loading responsibilities are currently handled in `src/app/useAppState.ts` and `src/app/AppShell.vue`.

This folder remains reserved for a future extraction if tournament-specific UI or loaders grow beyond app-shell scope.
