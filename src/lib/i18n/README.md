# I18n Library

Locale bootstrapping and translation helpers live here.

- `index.ts` initializes Vue I18n with app messages.
- `labels.ts` resolves tournament and ship labels without hardcoding them in features.
